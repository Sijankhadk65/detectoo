meta {
  name: Update Care Task
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/care-task/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "done": true
  }
}
