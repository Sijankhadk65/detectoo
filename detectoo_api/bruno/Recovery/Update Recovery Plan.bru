meta {
  name: Update Recovery Plan
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/recovery-plan/{{recovery_plan_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "progress": 0.5,
    "summary": "Recovery progressing — halfway through"
  }
}
